# sync-open
Create static server and open major browsers

## Install
```shell
npm install sync-open --save-dev
```

## Usage
```node
var so = require('sync-open');
var $ServerPath = "./dist";

so($ServerPath);
```

